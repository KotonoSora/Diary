package com.kotonosora.todolist.common

import java.util.Locale

object AppConstants {
    /**
     * Default App Locale used across the application for formatting dates, times, numbers, and UI strings.
     * All UI labels and time locales strictly use English (Locale.US).
     */
    val APP_LOCALE: Locale = Locale.US
}
