#include <jni.h>
#include <string>

// native-lib.cpp — reserved for future native utility functions.
// The core JNI implementations (sortStrings, parseMdTitle, filterByPrefix)
// live in todo-native.cpp.
